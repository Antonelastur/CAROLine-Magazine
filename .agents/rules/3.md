---
trigger: always_on
---

# În cazul imaginilor mari, rulează acest script ÎNTÂI
from PIL import Image
import os

def optimize_image(input_path, max_size_mb=4):
    img = Image.open(input_path)
    
    # Resize dacă e prea mare
    max_dimension = 2000
    if max(img.size) > max_dimension:
        img.thumbnail((max_dimension, max_dimension), Image.LANCZOS)
    
    # Salvează optimizat
    output_path = input_path.replace('.jpg', '_optimized.jpg')
    img.save(output_path, 'JPEG', quality=85, optimize=True)
    
    # Verifică dimensiune
    size_mb = os.path.getsize(output_path) / (1024*1024)
    print(f"Imaginea optimizată: {size_mb:.2f} MB")
    
    return output_path
```

## Ce să faci ACUM

1. **Identifică imaginea problematică** - probabil una din cele două pe care le-ai încărcat recent
2. **Comprimă-o** folosind metodele de mai sus
3. **Re-încarcă** versiunea comprimată
4. **Încearcă din nou** taskul

## Prevenție pentru viitor

Adaugă în regulile agentului:
```
⚠️ LIMITE TEHNICE - IMAGINI

- Maximum 5 MB per imagine
- Rezoluție recomandată: 2000x2000px pentru design
- Format preferat: JPEG (85% quality)
- Dacă primești imagine > 5MB → cere versiune optimizată
- NICIODATĂ nu încerca să procesezi direct imagini > 5MB